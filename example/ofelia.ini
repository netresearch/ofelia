[global]
enable-web = true

[job-run "run-date"]
schedule = @every 30s
image = alpine:latest
command = date

[job-local "local-echo"]
schedule = @every 45s
command = echo "Hello from local job"

# This job demonstrates the service-run type and requires Docker Swarm
# Uncomment when running in a Swarm cluster
#[job-service-run "swarm-example"]
#schedule = 0,20,40 * * * *
#image = alpine
#command = touch /tmp/swarm-example

# This job demonstrates compose type - requires a running compose stack
# Uncomment and adjust the file/service to match your setup
#[job-compose "pg-dump"]
#schedule = @daily
#file = compose.yml
#service = db
#command = pg_dumpall -U postgres
