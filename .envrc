# direnv project setup for Ofelia

# Load .env if present
if [ -f .env ]; then
  dotenv .env
fi

# Print helpful project info on enter
cat <<'EOF'
Welcome to Ofelia (Go job scheduler)

Useful commands:
- make fmt       # format Go files
- make vet       # run go vet
- make lint      # run golangci-lint
- make test      # run unit tests
- make ci        # vet + format check + tests
- make packages  # build cross-platform binaries into ./bin

Docs:
- README.md
- docs/architecture.md
- docs/jobs.md

Go version: $(go version 2>/dev/null || echo 'Go not found')
EOF

# Optional: set GOPATH bin on PATH for local tools
if command -v go >/dev/null 2>&1; then
  export PATH="$(go env GOPATH)/bin:$PATH"
fi
