version: 1
env:
  - CGO_ENABLED=0
  - GO111MODULE=on
flags:
  - -trimpath
goos: darwin
goarch: amd64
main: .
binary: ofelia-darwin-amd64
ldflags:
  - "-s -w"
  - "-X main.version={{ .Tag }}"
  - "-X main.build={{ .Commit }}"
