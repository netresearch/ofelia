version: 1
env:
  - CGO_ENABLED=0
  - GO111MODULE=on
flags:
  - -trimpath
goos: linux
goarch: 386
main: .
binary: ofelia-{{ .Os }}-{{ .Arch }}
ldflags:
  - "-s -w -X main.version={{ .Env.VERSION }} -X main.build={{ .Env.COMMIT }}"
