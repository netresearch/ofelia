version: 1
env:
  - CGO_ENABLED=0
  - GO111MODULE=on
flags:
  - -trimpath
goos: linux
goarch: 386
main: .
binary: ofelia-linux-386
ldflags:
  - "-s -w"
  - "-X main.version={{ .Tag }}"
  - "-X main.build={{ .Commit }}"
