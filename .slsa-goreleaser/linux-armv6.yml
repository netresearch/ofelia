version: 1
env:
  - CGO_ENABLED=0
  - GO111MODULE=on
  - GOARM=6
flags:
  - -trimpath
goos: linux
goarch: arm
main: .
binary: ofelia-linux-armv6
ldflags:
  - "-s -w"
  - "-X main.version={{ .Tag }}"
  - "-X main.build={{ .Commit }}"
